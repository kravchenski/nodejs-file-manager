# nodejs-file-manager
A simple Node.js-based file manager built in the terminal. It allows you to navigate through your file system, view files and directories, read/write files, create and remove directories — all from the command line interface.
